package com.draco.illud.utils

/*
 * This file just holds some declarations that are accessed in
 * most files, so it makes sense to just globalize them instead
 * of pass each one via reference to each class.
 */

/* Our global copies of classes */
val listItems = ListItems()